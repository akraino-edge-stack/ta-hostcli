[access]
	inheritFrom = ta
[access "refs/*"]
	owner = group ldap/akraino-gerrit-ta-hostcli-committers
[project]
	description = this repo holds the code for the pluggable AKREC CLI framework. The CLI connects to the infra REST API
